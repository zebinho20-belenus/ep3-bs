<?php

$backUrl = $this->url('backend/config/square');

$this->setup(array(
    'title' => 'Delete this square',
    'panel' => 'centered-panel',
    'back' => array(
        'Squares' => $backUrl,
    ),
));

?>

<div class="padded centered-text">
    <p><span class="symbolic symbolic-warning"><?= $this->t('Are you sure you want to delete this square?') ?></span></p>

    <div class="edit-actions">
        <a href="<?= $this->url('backend/config/square/delete', ['sid' => $this->sid], ['query' => ['confirmed' => 'true']]) ?>" class="default-button default-button-danger">
            <span class="symbolic symbolic-cross"><?= $this->t('Yes, delete this square') ?></span>
        </a>
        <a href="<?= $backUrl ?>" class="default-button default-button-outline"><?= $this->t('No, go back') ?></a>
    </div>

    <?php if ($this->squareBookings): ?>
        <p class="text-warning mt-3"><?= $this->t('Since this square has already bookings, it will be set to disabled but kept in the database') ?></p>
    <?php endif; ?>
</div>