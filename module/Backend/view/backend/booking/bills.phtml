<?php

$this->setup(array(
    'title' => 'Edit Booking-Bill',
    'panel' => 'centered-panel large-sized',
    'back' => array(
        'Booking-Bill' => $this->url('user/bookings/bills', ['bid' => $this->booking->need('bid')]),
    ),
));

$last_vat_gross = 1;
$last_vat_rate = 19;

?>

<div>
    <h1><?= $this->t('Edit Booking-Bill') ?></h1>

    <div class="separator-small"></div>

    <form method="post">
        <?= $this->csrfToken() ?>
        <div class="mb-3">
            <?= $this->t(sprintf('Status: %s',
                '<select name="ebf-status" class="form-select form-select-sm d-inline-block" style="width: auto;">' . $this->formSelect()->renderOptions(
                    $this->bookingStatusService->getStatusTitles(), array($this->booking->getBillingStatus())) . '</select>')) ?>
        </div>

        <div class="sandbox">
            <div class="table-responsive">
                <table class="table table-sm align-middle mb-0">
                    <thead>
                        <tr>
                            <th><?= $this->t('Description') ?></th>
                            <th class="right-text"><?= $this->t('Time (in minutes)') ?></th>
                            <th class="right-text"><?= $this->option('subject.square.unit.plural') ?></th>
                            <th><?= $this->t('Price (in cent)') ?></th>
                            <th class="text-center"><?= $this->t('Delete') ?></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($this->bills as $bill): ?>
                            <tr>
                                <td><input type="text" name="ebf-<?= $bill->need('bbid') ?>-description" value="<?= $this->escapeHtml($bill->get('description')) ?>" class="form-control form-control-sm" style="width: 220px;"></td>

                                <td class="right-text"><input type="text" name="ebf-<?= $bill->need('bbid') ?>-time" value="<?= ($bill->get('time') ? ($bill->get('time') / 60) : '') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                                <td class="right-text"><input type="text" name="ebf-<?= $bill->need('bbid') ?>-quantity" value="<?= $bill->get('quantity') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                                <td>
                                    <div class="d-flex align-items-center flex-wrap gap-1">
                                        <input type="text" name="ebf-<?= $bill->need('bbid') ?>-price" value="<?= $bill->get('price') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;">

                                        <?php
                                        $last_vat_gross = $bill->get('gross');
                                        $last_vat_rate = $bill->get('rate');

                                        echo '<select name="ebf-' . $bill->need('bbid') . '-vat-gross" class="form-select form-select-sm" style="width: auto;">';
                                        echo $this->formSelect()->renderOptions(array(
                                            '0' => $this->t('plus'),
                                            '1' => $this->t('including'),
                                        ), array(
                                            $last_vat_gross,
                                        ));
                                        echo '</select>';
                                        ?>

                                        <input type="text" name="ebf-<?= $bill->need('bbid') ?>-vat-rate" value="<?= $last_vat_rate ?>" class="form-control form-control-sm" style="width: 40px; text-align: right;">
                                        <span class="text-muted">% <?= $this->t('VAT') ?></span>
                                    </div>
                                </td>

                                <td class="text-center">
                                    <input type="checkbox" name="ebf-<?= $bill->need('bbid') ?>-delete" value="1" class="form-check-input" title="<?= $this->t('Delete') ?>">
                                </td>
                            </tr>
                        <?php endforeach; ?>

                        <tr class="table-light">
                            <td><input type="text" name="ebf-new-description" placeholder="<?= $this->t('New position') ?>" class="form-control form-control-sm" style="width: 220px;"></td>

                            <td class="right-text"><input type="text" name="ebf-new-time" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                            <td class="right-text"><input type="text" name="ebf-new-quantity" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                            <td>
                                <div class="d-flex align-items-center flex-wrap gap-1">
                                    <input type="text" name="ebf-new-price" class="form-control form-control-sm" style="width: 64px; text-align: right;">

                                    <?php
                                    echo '<select name="ebf-new-vat-gross" class="form-select form-select-sm" style="width: auto;">';
                                    echo $this->formSelect()->renderOptions(array(
                                        '0' => $this->t('plus'),
                                        '1' => $this->t('including'),
                                    ), array(
                                        $last_vat_gross,
                                    ));
                                    echo '</select>';
                                    ?>

                                    <input type="text" name="ebf-new-vat-rate" value="<?= $last_vat_rate ?>" class="form-control form-control-sm" style="width: 40px; text-align: right;">
                                    <span class="text-muted">% <?= $this->t('VAT') ?></span>
                                </div>
                            </td>

                            <td></td>
                        </tr>

                        <?php if (!empty($this->previewBills)): ?>
                            <?php foreach ($this->previewBills as $idx => $previewBill): ?>
                                <tr class="table-warning">
                                    <td>
                                        <input type="text" name="ebf-preview-<?= $idx ?>-description" value="<?= $this->escapeHtml($previewBill->get('description')) ?>" class="form-control form-control-sm" style="width: 220px;">
                                        <small class="text-muted"><?= $this->t('Preview - click Save to keep') ?></small>
                                    </td>

                                    <td class="right-text"><input type="text" name="ebf-preview-<?= $idx ?>-time" value="<?= ($previewBill->get('time') ? ($previewBill->get('time') / 60) : '') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                                    <td class="right-text"><input type="text" name="ebf-preview-<?= $idx ?>-quantity" value="<?= $previewBill->get('quantity') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;"></td>

                                    <td>
                                        <div class="d-flex align-items-center flex-wrap gap-1">
                                            <input type="text" name="ebf-preview-<?= $idx ?>-price" value="<?= $previewBill->get('price') ?>" class="form-control form-control-sm" style="width: 64px; text-align: right;">

                                            <?php
                                            echo '<select name="ebf-preview-' . $idx . '-vat-gross" class="form-select form-select-sm" style="width: auto;">';
                                            echo $this->formSelect()->renderOptions(array(
                                                '0' => $this->t('plus'),
                                                '1' => $this->t('including'),
                                            ), array(
                                                $previewBill->get('gross'),
                                            ));
                                            echo '</select>';
                                            ?>

                                            <input type="text" name="ebf-preview-<?= $idx ?>-vat-rate" value="<?= $previewBill->get('rate') ?>" class="form-control form-control-sm" style="width: 40px; text-align: right;">
                                            <span class="text-muted">% <?= $this->t('VAT') ?></span>
                                        </div>
                                    </td>

                                    <td></td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>

                        <tr>
                            <td colspan="5">
                                <button type="submit" name="ebf-create-default" value="1" class="btn btn-link symbolic symbolic-plus-link p-0"><?= $this->t('New position by using the pricing rules for this booking') ?></button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="edit-actions">
            <input type="submit" name="ebf-save" value="<?= $this->t('Save') ?>" class="default-button">
            <input type="submit" name="ebf-save-and-back" value="<?= $this->t('Save and back') ?>" class="default-button default-button-outline">
        </div>
    </form>
</div>
